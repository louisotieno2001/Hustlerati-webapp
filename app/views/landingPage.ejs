<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome | Hustlerati</title>
    <link rel="icon" href="/images/logo1.png" type="image/x-icon">
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() { dataLayer.push(arguments); }
        gtag('js', new Date());

        gtag('config', 'G-VCMHYS7QJ4');
    </script>
    <style>
        @font-face {
            font-family: JuneBug;
            src: url('JUNEBUG.TTF');
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background-color: #5d4037;
            color: #fff;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: transparent;
        }

        .logo {
            font-size: xx-large;
            font-weight: bold;
            color: #BC8D00;
        }

        .button {
            padding: 10px 20px;
            border: none;
            border-radius: 25px;
            color: white;
            cursor: pointer;
            font-size: 16px;
            margin-left: 10px;
            transition: background-color 0.3s ease;
        }

        .login {
            background-color: #BC8D00;
            text-decoration: none;
        }

        .signup {
            background-color: #BC8D00;
            text-decoration: none;
        }

        .button:hover {
            opacity: 0.9;
        }

        .welcome {
            flex: 1;
            display: flex;
            justify-content: left;
            flex-direction: column;
            justify-content: space-between;
            padding: 20px;
            margin: 30px;
            height: 70vh;
            flex-direction: row;
        }

        .text {
            width: 50%;
            height: 70vh;
            font-size: xx-large;
        }

        .image {
            background-image: url('/images/shopping.avif');
            background-size: cover;
            background-position: center;
            border-radius: 15px;
            width: 50%;
            height: 70vh;
        }

        .cookie-banner {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background-color: #450F01;
            color: #fff;
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.2);
            z-index: 1000;
            color: #fff;
        }

        .cookie-banner p {
            margin: 0;
        }

        .cookie-banner button {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 0 5px;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .cookie-banner button.reject {
            background-color: #f44336;
        }

        .social-nav {
            display: flex;
            justify-content: center;
            padding: 10px;
        }

        .social-nav a {
            margin: 0 10px;
            color: #BC8D00;
            text-decoration: none;
        }

        .social-nav img {
            margin: 0 5px;
            width: 30px;
            height: 30px;
        }

        .explain {
            font-family: 'MyWebFont', Fallback, sans-serif;
            font-style: italic;
            font-weight: lighter;
            font-size: xx-large;
        }

        .title {
            color: #BC8D00;
        }

        .suggestions {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 2rem;
            width: 80%;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        .tabs {
            display: flex;
            justify-content: flex-start;
            margin-bottom: 1rem;
            overflow-x: auto;
            white-space: nowrap;
            width: 100%;
            scrollbar-width: none;
            /* Firefox */
            -ms-overflow-style: none;
            /* Internet Explorer and Edge */
        }

        .tabs::-webkit-scrollbar {
            display: none;
            /* Chrome, Safari, and Opera */
        }

        .tab-btn {
            background-color: #6D4C41;
            border: none;
            outline: none;
            cursor: pointer;
            padding: 0.5rem 1rem;
            margin-right: 0.5rem;
            font-size: 1rem;
            transition: background-color 0.3s ease;
            border-radius: 8px;
            color: #f1f1f1;
            flex-shrink: 0;
        }

        .tab-btn.active {
            background-color: #3E2723;
        }

        .tab-btn::before {
            content: "";
            display: inline-block;
            width: 20px;
            height: 20px;
            background-size: contain;
            background-repeat: no-repeat;
            margin-right: 0.5rem;
            vertical-align: middle;
        }

        .tab-btn:nth-child(1)::before {
            background-image: url("/images/trending-searches.png");
        }

        .tab-btn:nth-child(2)::before {
            background-image: url("/images/top-searches.png");
        }

        .tab-btn:nth-child(3)::before {
            background-image: url("/images/fashion.png");
        }

        .tab-btn:nth-child(4)::before {
            background-image: url("/images/electronics.png");
        }

        .tab-btn:nth-child(5)::before {
            background-image: url("/images/sports.png");
        }

        .tab-content {
            width: 80%;
            background-color: transparent;
            padding: 1rem;
        }

        .tab-pane {
            display: none;
        }

        .tab-pane.active {
            display: block;
        }

        @media (max-width: 768px) {
            .suggestions {
                width: 90%;
            }

            .tab-btn {
                padding: 0.5rem;
                margin-right: 0.25rem;
                font-size: 0.9rem;
            }

            .tab-btn::before {
                width: 16px;
                height: 16px;
                margin-right: 0.25rem;
            }
        }

        @media screen and (max-width: 700px) {
            .logo {
                font-size: x-large;
                font-weight: bold;
                color: #BC8D00;
            }

            .welcome {
                flex: 1;
                display: flex;
                justify-content: left;
                flex-direction: column;
                justify-content: space-between;
                padding: 20px;
                margin: 3px;
                height: 70vh;
            }

            .explain {
                font-family: 'MyWebFont', Fallback, sans-serif;
                font-style: italic;
                font-size: x-large;
                margin: 5px;
            }

            .title {
                color: #BC8D00;
            }

            .text {
                width: 100%;
                height: 70vh;
                font-size: lighter;
            }

            .image {
                background-image: url('/images/shopping.avif');
                background-size: cover;
                background-position: center;
                border-radius: 15px;
                width: 100%;
                height: 70vh;
            }
        }
    </style>
</head>

<body>
    <header>
        <div class="logo">Hustlerati</div>
        <div>
            <a href="/login" class="button login" onclick="login()">Login</a>
            <a href="/register" class="button signup" onclick="signup()">Signup</a>
        </div>
    </header>

    <div class="welcome">
        <div class="text">
            <h1 class="title">Karibu Hustlerati!</h1>
            <h1 class="explain">Sell and Get all your fashion, art and electronics needs met today. Tomorrow is too far!
            </h1>
        </div>
        <div class="image">

        </div>
    </div>

    <div class="cookie-banner" id="cookieBanner">
        <p>We use cookies to improve your experience on our website. By continuing to browse, you agree to our use of
            cookies.</p>
        <div>
            <button class="accept" onclick="acceptCookies()">Accept</button>
            <button class="reject" onclick="rejectCookies()">Reject</button>
        </div>
    </div>
    <div class="suggestions">
        <div class="tabs">
            <button class="tab-btn active">Trending</button>
            <button class="tab-btn">Top Searches</button>
            <button class="tab-btn">Fashion</button>
            <button class="tab-btn">Electronics</button>
            <button class="tab-btn">Sports</button>
        </div>
        <div class="tab-content">
            <div class="tab-pane active" id="trending">
                <h2>Nothing yet in Trending-searches</h2>
            </div>
            <div class="tab-pane" id="top-searches">
                <h2>Nothing yet in Top-searches</h2>
            </div>
            <div class="tab-pane" id="fashion">
                <h2>Nothing yet in Fashion</h2>
            </div>
            <div class="tab-pane" id="electronics">
                <h2>Nothing yet in Electronics</h2>
            </div>
            <div class="tab-pane" id="sports">
                <h2>Nothing yet in Sports</h2>
            </div>
        </div>
    </div>

    <nav class="social-nav">
        <a aria-label="Chat on WhatsApp" href="https://wa.me/254746852485"><img src="/images/icon-whatsapp.webp"
                alt="Whatsapp"></a>
        <a href="https://www.tiktok.com/@hustlerati_?lang=en"><img src="/images/tiktok-icon.webp" alt="Linked In"></a>
        <a href="#"><img src="/images/facebook.png" alt="Facebook"></a>
        <a href="https://x.com/Hustlerati30397"><img src="/images/x.jpg" alt="X"></a>
        <a href="https://www.instagram.com/hustlerati.africa/?hl=en"><img src="/images/instagram.png"
                alt="Instagram"></a>
        <a href="https://www.linkedin.com/in/hustlerati-africa-124b8b338/"><img src="/images/linkedin.png"
                alt="Linked In"></a>
    </nav>
    <p style="text-align: center; color: yellow;">Copyright &COPY; Hustlerati 2024 </p>
    <script>
        // Get the tab buttons and tab content container
        const tabBtns = document.querySelectorAll('.tab-btn');
        const tabPanes = document.querySelectorAll('.tab-pane');

        // Add event listeners to the tab buttons
        tabBtns.forEach((btn, index) => {
            btn.addEventListener('click', () => {
                // Remove active class from all buttons and panes
                tabBtns.forEach(b => b.classList.remove('active'));
                tabPanes.forEach(p => p.classList.remove('active'));

                // Add active class to the clicked button and corresponding pane
                btn.classList.add('active');
                tabPanes[index].classList.add('active');
            });
        });

        // Function to set a cookie
        function setCookie(name, value, days) {
            let expires = "";
            if (days) {
                const date = new Date();
                date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
                expires = "; expires=" + date.toUTCString();
            }
            document.cookie = name + "=" + (value || "") + expires + "; path=/";
        }

        // Function to get a cookie value
        function getCookie(name) {
            const nameEQ = name + "=";
            const ca = document.cookie.split(';');
            for (let i = 0; i < ca.length; i++) {
                let c = ca[i];
                while (c.charAt(0) == ' ') c = c.substring(1, c.length);
                if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length, c.length);
            }
            return null;
        }

        // Function to accept cookies
        function acceptCookies() {
            setCookie('cookieConsent', 'accepted', 30);
            document.getElementById('cookieBanner').style.display = 'none';
        }

        // Function to reject cookies
        function rejectCookies() {
            window.location.href = 'https://www.google.com';
        }

        // Check for cookie consent on page load
        window.onload = function () {
            const cookieConsent = getCookie('cookieConsent');
            if (cookieConsent === 'accepted') {
                document.getElementById('cookieBanner').style.display = 'none';
            } else {
                document.getElementById('cookieBanner').style.display = 'flex';
            }
        };
    </script>
</body>

</html>